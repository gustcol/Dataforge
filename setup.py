#!/usr/bin/env python
"""
DataForge Setup Script

This setup.py is provided for backwards compatibility with older pip versions.
The project configuration is primarily defined in pyproject.toml.

For development installation:
    pip install -e ".[dev]"

For full installation with all features:
    pip install -e ".[all]"

Author: DataForge Team
"""

from setuptools import setup

if __name__ == "__main__":
    setup()
